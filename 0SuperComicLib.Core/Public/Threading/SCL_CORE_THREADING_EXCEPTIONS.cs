namespace SuperComicLib.Threading
{
    internal static class SCL_CORE_THREADING_EXCEPTIONS
    {
        public const string INVALID_TIMEOUT_VALUE = "Invalid timeout value";

        public const string INVALID_COUNT = "Bad thread exit";
    }
}
